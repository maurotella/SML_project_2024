\section{Training}

In this section, the training phase of the model is described, followed by an evaluation of performance on real robot navigations.

Mean Squared Error (MSE) was chosen as loss function; other functions like MAE or Huber loss were tested but showed lower performance than MSE. The training duration was 50 epochs. $k-$fold Cross Validation ($k-$CV) was used to estimate the generalization performance and to mitigate overfitting. For each fold, the model was trained on $k-1$ subsets, always with 50 epochs, and validated on the remaining one, rotating through all folds. This approach ensures that each data point is used for both training and validation, providing a more robust evaluation of model performance. A $k$ value of 5 was used.

Each model was trained from scratch through the Stochastic Gradient Descent (SGD) optimizer with learning rate and momentum found in the previous section and using batch size and dropout found in the previous section.

\subsection{Training results}
\subsubsection{Prediction performance}
Performance results are shown in Table \ref{tab:training} in terms of losses and Table \ref{tab:r2score} in terms of $R^2$-Score, RMSE and NRMSE. Looking at the first table, SqueezeNet outperforms the others in all losses
except for the $5-$CV loss, suggesting a worse generalization of the model.
AlexNet and MobileNetV3 show similar results in all losses with a slight advantage of the latter and a good generalization.

\begin{table}[H]
    \centering
    \begin{tabular}{|r|c|c|c|c|} \hline
        \textbf{Models} & \textbf{Train loss}   & \textbf{Valid. Loss}   & \textbf{Test Loss} & \textbf{$5-$CV Loss} \\\hline
        AlexNet     & 0.01804 & 0.01808 & 0.01691 & 0.01718 \\\hline
        MobileNetV3 & 0.01615 & 0.01807 & 0.01718 & \color{red}{0.01667}  \\\hline
        SqueezeNet  & \color{red}{0.01340} & \color{red}{0.01437} & \color{red}{0.01309} & 0.02040 \\\hline
    \end{tabular}
    \caption{Models performances in terms of loss values\label{tab:training}}
\end{table}

\noindent
In Table \ref{tab:r2score} the following metrics are used:
\[
\begin{array}{r@{\hspace{1.1cm}}r@{\hspace{1.1cm}}r}
    R^2 = 1-\displaystyle\frac{\sum_i(y_i-\hat{y}_i)^2}{\sum_i(y_i-\overline{y})^2} &
    \text{RMSE} = \displaystyle\sqrt{\frac{\sum_i(y_i-\hat{y}_i)^2}{n}} &
    \text{NRMSE} = \displaystyle\frac{\text{RMSE}}{\max_i \hat{y}_i - \min_i \hat{y}_i}
\end{array}
\]
where $y_i$ is the $i$-th real value and $\hat{y}_i$ its prediction, $\overline{y}$ the mean of the real values and $n$ the number of data points.

Here again SqueezeNet outperforms the other models in all cases except NRMSE for ATE where it still gets a good value compared to the others. AlexNet has Pareto worst performance while MobileNetV3 stays in the middle.

\begin{table}[H]\centering
\begin{tabular}{r|ccc|ccc|}
\cline{2-7}
\multicolumn{1}{l|}{}                 & \multicolumn{3}{c|}{\textbf{ATE}}                                                                           & \multicolumn{3}{c|}{\textbf{ARE}}                                                                                                                           \\ \hline
\multicolumn{1}{|r|}{\textbf{Models}} & \multicolumn{1}{c|}{$\bm{R^2}$} & \multicolumn{1}{c|}{\textbf{RMSE}} & \multicolumn{1}{c|}{\textbf{NRMSE}} & \multicolumn{1}{c|}{$\bm{R^2}$} & \multicolumn{1}{c|}{\textbf{RMSE}} & \multicolumn{1}{c|}{\textbf{NRMSE}} \\ \hline
\multicolumn{1}{|r|}{Alexnet}         & \multicolumn{1}{c|}{0.4}            & \multicolumn{1}{c|}{1.19}              &        0.28                          & \multicolumn{1}{c|}{0.81}            & \multicolumn{1}{c|}{0.017}              &                 0.086                                        \\ \hline
\multicolumn{1}{|r|}{MobileNetV3}       & \multicolumn{1}{c|}{0.46}            & \multicolumn{1}{c|}{1.13}              & \color{red}0.164                           & \multicolumn{1}{c|}{0.85}            & \multicolumn{1}{c|}{0.016}              &       0.078                                                      \\ \hline
\multicolumn{1}{|r|}{SqueezeNet}      & \multicolumn{1}{c|}{\color{red}0.5}            & \multicolumn{1}{c|}{\color{red}1.09}              &          0.19                       & \multicolumn{1}{c|}{\color{red}0.92}            & \multicolumn{1}{c|}{\color{red}0.011}              &  \color{red}0.056                                                     \\ \hline
\end{tabular}
\captionsetup{width=0.93\linewidth}
\caption{Performance comparison in terms of $R^2$-Score, RMSE ($m$) and NRMSE (\% with respect to the range of predicted values)\label{tab:r2score}}
\end{table}

\noindent
The learning curves obtained during the training phase are shown in Figure \ref{fig:learnCurve}. The $5-$CV loss and the test loss are highlighted, providing a visual comparison of the models generalization capabilities and their performance on unseen data. 

In all models no evident signs of overfitting are present as the validation loss never deviates too much from the training loss. AlexNet has the smoothest descent. MobileNetV3 in the first 25 epochs shows a small gap between validation and training loss which, however, is narrowing in later epochs. In both AlexNet and MobileNetV3 test and $5-$CV loss are very similar and close to the learning curves indicating good generalization of the models as the models perform well on both seen and unseen data. SqueezeNet shows a similar situation with a higher $5-$CV indicating a possible lower capacity of the model to generalize.


\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{figures/learning_curves.pdf}
    \includegraphics[width=.5\textwidth]{figures/learning_curves2.pdf}
    \caption{Learning curves of models; highlighted also the $5-$CV loss and the test loss\label{fig:learnCurve}}
\end{figure}

\subsubsection{Efficency}
Regarding efficiency, in terms of inference time and model size, shown in Table \ref{tab:training_efficency}, as expected, SqueezeNet is the most lightweight model and AlexNet, by far, the heaviest.

MobileNetV3 turns out to be, in general, the fastest model with the lowest CPU time and a slightly slower GPU than AlexNet, which, counterintuitively, is the fastest in terms of GPU time. The latter fact is mainly due to the simple structure of the model, consisting of only basic operations that are highly optimized in modern GPU.

So, overall, MobileNetV3 finishes as the most efficient model among those seen in this work.

Inference times were calculated by averaging the individual datapoint inference times over the test set. Only  batchs of one element are used. This because we wanted to analyze inference times in real time applications, where what matters is immediate responsiveness and not throughput.

\begin{table}[H]
    \centering
    \begin{tabular}{|r|c|c|c|} \hline
        \textbf{Models} & \textbf{tCPU(ms)}   & \textbf{tGPU(ms)}   & \textbf{Size(MB)} \\\hline
        AlexNet     & 16.8166 & \color{red} 0.2523 & 142.3 \\\hline
        MobileNetV3 & \color{red}1.4469 & 0.5893 & 5.86 \\\hline
        SqueezeNet  & 3.1651 & 0.3553 & \color{red}2.76 \\\hline
    \end{tabular}
    \caption{Models inference times with CPU and GPU and sizes\label{tab:training_efficency}
    \label{fig:learningcurve}}
\end{table}

\subsection{SLAM predicition evaluation}
It should be highlighted that all models have a very high RMSE for the ATE, as Table \ref{tab:r2score} shows. An error of 1 $m$, when used to correct SLAM prediction of trajectories, could be too high and would irreparably damage the robot's navigation. To understand this problem, a small analysis was conducted.

\subsubsection{Analysis of high RMSE}
After rechecking the data, it was found that in many cases the ATE values, used as real values, were derived from \quotes{broken} simulations. To clarify the meaning of \quotes{broken}, let see Figure \ref{fig:high_ATE_traj}. It is evident that, at some point, something went wrong and the SLAM trajectory, in blue, became corrupted (left figure). This leads to having such a high ATE values (central figure). One might think that this leads to having, as a result, an unusable map, however, the map obtained (right figure) turns out to be accurate.
\begin{figure}[H]
    \begin{minipage}{0.31\textwidth}
        \includegraphics[width=\textwidth]{figures/high_ATE.pdf}
    \end{minipage}
    \begin{minipage}{0.4\textwidth}
        \includegraphics[width=\textwidth]{figures/high_ATE_plot.pdf}
    \end{minipage}
    \begin{minipage}{0.25\textwidth}
        \includegraphics[width=\textwidth]{figures/high_ATE_map.png}
    \end{minipage}
    \captionsetup{width=0.95\linewidth}
    \caption{Comparison of real and SLAM trajectories (left), ATE over time (center) and final SLAM map (center) in a simulation\label{fig:high_ATE_traj}}
\end{figure}

\noindent
In this way, all datapoints that fall into this situation, are inconsistent. With a quick test it turned out that the inconsistent datapoints are $\sim$22\% of the dataset and this probably led the RMSE (Table \ref{tab:r2score}) to be so high.

A final investigation found that detecting whether a map is accurate or not, is still an open problem; Birk \cite{birk2010quantitative} introduced the concept of brokeness which aims to quantitatively assess the quality of SLAM-generated maps. Although this metric is useful for a discussion of map quality, it remains a qualitative tool. So, the only accurate way to understand if a map is accurate or not is through manual supervision. For this reason, cleaning this type of dataset is a time-consuming task.
